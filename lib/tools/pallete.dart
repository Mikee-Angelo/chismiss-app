import '../widgets.dart';

class Pallete {
  static const primary = Color.fromRGBO(180, 248, 200, 1);
  static const secondary = Color.fromRGBO(255, 174, 188, 1);
  static const tertiary = Color.fromRGBO(251, 231, 198, 1);
}
